# Orchestration

The orchestration layer coordinates the project assets and refresh flow.

It keeps the pipeline reliable by:
- defining asset dependencies
- scheduling recurring updates
- tying the data stack together in one workflow
