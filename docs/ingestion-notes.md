# Ingestion notes

The ingestion layer is responsible for bringing market data into the platform from external providers.

Key responsibilities:
- Connect to the exchange feed
- Transform raw events into a consistent shape
- Publish data into the downstream streaming system
