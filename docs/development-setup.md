# Development setup

Use the local Docker compose stack and Python environment to run the platform components together.

Recommended flow:
- Start the shared infrastructure services from the repository root.
- Run ingestion and streaming services separately when needed.
- Use the ML and dashboard components for inference and exploration.
